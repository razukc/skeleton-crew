{
  "name": "agent-buildoff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest --run",
    "experiment": "npm run build && node dist/harness/run.js"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "skeleton-crew": "file:../.."
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
